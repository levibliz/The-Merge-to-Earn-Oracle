# Network
RPC_URL=https://soroban-testnet.stellar.org
NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Deployer secret key (DO NOT commit real keys)
DEPLOYER_SECRET_KEY=S...

# Oracle address (the backend's public key) — set in contract initialize()
ORACLE_ADDRESS=G...

# Token contract address (e.g. wrapped XLM SAC)
TOKEN_ADDRESS=C...

# Optional: WASM hash for contract verification
# WASM_HASH=...
